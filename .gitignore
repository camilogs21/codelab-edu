# Sistema operacional
.DS_Store
Thumbs.db

# Editores
.vscode/*
!.vscode/extensions.json
.idea/

# Dependências (caso algum sprint futuro introduza bundler/ferramentas)
node_modules/
package-lock.json

# Build (caso vire necessário empacotar Monaco/xterm localmente no Sprint 6)
dist/
build/

# Variáveis de ambiente / segredos (a chave da API de compilação do Sprint 3
# NUNCA deve ser versionada — ela vive só no proxy serverless, fora deste repo)
.env
.env.local

# Logs
*.log
