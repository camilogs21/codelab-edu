/**
 * achievements.js
 * Sprint 1: contrato vazio. Sprint 5 (Gamificação) implementa a lista de
 * conquistas, a lógica de desbloqueio e o toast visual (ver `.toast` em
 * animations.css, já pronto para ser reutilizado aqui).
 */

/** Lista de conquistas do aluno. Sprint 5 lê isso de storage.js. */
export function getUnlockedAchievements() {
  return [];
}

/** Verifica e desbloqueia conquistas com base num evento (ex.: "primeiro
 *  código executado", "5 exercícios seguidos sem erro"). Implementado
 *  no Sprint 5. */
export function checkAchievement(_eventName, _payload) {
  // Intencionalmente vazio no Sprint 1.
}
