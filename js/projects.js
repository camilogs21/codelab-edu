/**
 * projects.js
 * Sprint 1: contrato vazio. Área de projetos guiados do briefing
 * (Calculadora, Agenda, Biblioteca, Sistema Escolar, Jogo da Forca,
 * Pedra-Papel-Tesoura, Banco, Mercado) — implementada em um sprint
 * dedicado após o sistema de aulas (Sprint 4), já que projetos reaproveitam
 * a mesma estrutura de exercício + avaliação.
 *
 * Cada projeto seguirá: { id, title, description, difficulty, starterCode,
 * requirements[], rubric }. Os arquivos-fonte de cada projeto (por
 * linguagem) vivem em /data/projects/, não neste módulo.
 */

/** Lista de projetos disponíveis. Hoje vazio — populado a partir de
 *  /data/projects/*.json quando esse sprint for feito. */
export function getAvailableProjects() {
  return [];
}
